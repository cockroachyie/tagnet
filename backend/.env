# Flask application settings
FLASK_ENV=development
FLASK_DEBUG=True
FLASK_APP=app.py

# The host and port Flask runs on
HOST=0.0.0.0
PORT=5000

# Optional: CORS allowed origins
CORS_ORIGINS=http://localhost:3000
